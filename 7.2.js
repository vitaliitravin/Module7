const person = {
    name: 'Vlad',
    age: 26
  }

  const str = 'string';

  const hasNameProp = 'name' in person;
  console.log(hasNameProp);

  const hasAgeProp = 'age' in person;
  console.log(hasAgeProp);


  ///Просьба показать как выполнить задание согласно условию задачи - с пременением функции и проверкой строки?